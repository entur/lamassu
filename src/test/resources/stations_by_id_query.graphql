{
    stationsById(
        ids: ["TST:Station:1"]
    ) {
        id
        name {
            translation {
                language
                value
            }
        }
        lat
        lon
        address
        capacity
        numBikesAvailable
        vehicleTypesAvailable {
            vehicleType {
                formFactor
                propulsionType
            }
            count
        }
        numDocksAvailable
        isInstalled
        isRenting
        isReturning
        lastReported
        system { id }
        pricingPlans { id }
    }
}
