{
    stationsById(
        ids: ["TST:Station:1"]
    ) {
        id
        lat
        lon
        capacity
        numBikesAvailable
        numDocksAvailable
        isInstalled
        isRenting
        isReturning
        lastReported
        system { id }
        pricingPlans { id }
    }
}
