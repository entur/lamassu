{
    stationsById(
        ids: ["TST:Station:1"]
    ) {
        id
        name
        lat
        lon
        address
        capacity
        numBikesAvailable
        numDocksAvailable
        isInstalled
        isRenting
        isReturning
        lastReported
        system { id }
        pricingPlans { id }
    }
}
